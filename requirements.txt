# Ecobee Temperature Management Service Dependencies

# HTTP requests
requests==2.33.1

# Timezone support
pytz==2026.1

# Web framework for health endpoint
Flask==3.1.3

# Werkzeug (Flask dependency, pinned for compatibility)
Werkzeug==3.1.8

# Production WSGI server (replaces Flask dev server)
waitress==3.0.2

# Browser automation for JWT extraction via Ecobee web portal login
selenium==4.41.0

# JWT signature verification via ecobee JWKS endpoint
PyJWT==2.10.1
cryptography==45.0.2
